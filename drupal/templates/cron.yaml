apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ .Release.Name }}-cron
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  backOffLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: drupal-cron
<<<<<<< HEAD
            command: ["/bin/sh", "-c"]
            args:
            - {{ .Values.commands.cron | quote }}
{{ include "drupal.drupal-container" . | indent 12 }}
          imagePullSecrets:
          - name: gcr
          volumes:
{{ include "drupal.drupal-files-volume" . | indent 10 }}
=======
            image: "{{ .Values.drupal.image }}"
            command:
            - drush
            - cron
            env:
            - name: PHP_FPM_CLEAR_ENV
              value: "no"
            - name: DB_USER
              value: "{{ .Values.mariadb.db.user }}"
            - name: DB_NAME
              value: "{{ .Values.mariadb.db.name }}"
            - name: DB_HOST
              value: "{{ .Release.Name }}-mariadb"
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: "{{ .Release.Name }}-mariadb"
                  key: mariadb-password
          imagePullSecrets: {{ .Values.secrets.imagePullSecrets }}        
>>>>>>> imagePullSecret
          restartPolicy: OnFailure
